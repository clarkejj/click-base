import pytest, json
from pprint import pprint


def test_wellformedjson():

    data = json.load(open('data.json'))
#    pprint(data)
    data["maps"][0]["id"]
    data["masks"]["id"]
    data["om_points"]
    assert bool(1) is True
